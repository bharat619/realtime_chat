<% flash.each do |type, content|%>
  <div class="ui message <%= type %> transition transition">
    <i class="close icon"></i>
    <div class="header">
      <%= content %>
    </div>
  </div>
<%end%>
